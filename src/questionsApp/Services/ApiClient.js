/*
* API Client is a wrapper to wrap all the common API related requirements throughout the app
*/

import data from './data';

const ApiClient = (endPoint, params = {}, config = {}) => {
  return Promise.resolve(data);
};

export default ApiClient;
